int main():
    int a = 50, b = 0, c = 0
    while a != 1:
        b = ( a % 2 == 0 ) ? a / 2 : 3 * a + 1
        while a != b:
            if a > b:

                a -= b
            else:
                b -= a
            end //if ends here
        end //while ends here
        c = a
        print(c)
        a = b
    end //while ends here
    return 0
end //function ends here